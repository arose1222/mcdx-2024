<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Opportunity_Amount_Required_When_Won</fullName>
    <active>true</active>
    <description>Requires that the Amount field is populated and greater than zero when an opportunity is marked as Closed Won.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(StageName, &quot;Closed Won&quot;),
    OR(
        ISBLANK(Amount),
        Amount &lt;= 0
    )
)</errorConditionFormula>
    <errorDisplayField>Amount</errorDisplayField>
    <errorMessage>Amount must be greater than zero when marking an opportunity as Closed Won.</errorMessage>
</ValidationRule>
